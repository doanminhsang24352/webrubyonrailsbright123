<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <style>
      /* Style cho menu */
      /* Style cho menu */
      /* Style cho menu */
      /* Style for the navbar */
.navbar {
  background-color: #333;
  display: flex; /* Use flexbox to make the items appear in a row */
  justify-content: flex-start; /* Align items to the left */
  align-items: center; /* Center items vertically */
  padding: 0; /* Remove padding to avoid extra space around the navbar */
  # padding-left: 260px; 
  margin-right: 110px;
}

.navbar a, .navbar .navbar-link {
  color: white;
  text-align: center;
  padding: 14px 20px; /* This padding is optional, can adjust if necessary */
  text-decoration: none;
  margin: 0; /* Remove any margin */
}

.navbar a:hover, .navbar .navbar-link:hover {
  background-color: #ddd;
  color: black;
}

/* Ensure button matches link styles */
.navbar button.navbar-link {
  background-color: #333; /* Same as the navbar background */
  border: none;
  color: white;
  text-align: center;
  padding: 14px 20px;
  text-decoration: none;
  cursor: pointer;
}

.navbar button.navbar-link:hover {
  background-color: #ddd;
  color: black;
}

/* Optional: Remove extra space between menu items */
.navbar {
  gap: 0; /* Remove space between items */
}


      /* Style cho flash messages */
      .flash.notice {
        background-color: #dff0d8;
        color: #3c763d;
        padding: 10px;
        border: 1px solid #d6e9c6;
        margin: 10px 0;
      }

      .flash.alert {
        background-color: #f2dede;
        color: #a94442;
        padding: 10px;
        border: 1px solid #ebccd1;
        margin: 10px 0;
      }

    </style>
  </head>

  <body>
    <!-- Menu -->
    <nav class="navbar">
      <a href="/">Trang chủ</a>
      <a href="/products">Danh sách sản phẩm</a>
      <a href="/products/new">Thêm sản phẩm mới</a>

      <%# Hiển thị nút Đăng xuất nếu người dùng đã đăng nhập %>
      <%= button_to "Log out", session_path, method: :delete, class: 'navbar-link' if authenticated? %>

      <%# Hiển thị liên kết Đăng nhập nếu người dùng chưa đăng nhập %>
      <%= link_to "Login", new_session_path, class: 'navbar-link' unless authenticated? %>
    </nav>

    <%# Hiển thị thông báo flash %>
    <div id="flash-messages">
      <% if notice %>
        <div class="flash notice"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="flash alert"><%= alert %></div>
      <% end %>
    </div>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
